version: '3.7'

services:
  deploy:
    build: .
    image: ${SWARMLET_REGISTRY}/env
    secrets:
      - swarmlet-user-secrets
    configs:
      - swarmlet-config
    deploy:
      restart_policy:
        condition: none

secrets:
  swarmlet-user-secrets:
    external: true

configs:
  swarmlet-config:
    external: true
