#!/usr/bin/env bash

set -eo pipefail; [[ $DEBUG ]] && set -x

deploy() {
  declare cmd="deploy"
  [[ "$1" == "$cmd" ]] && shift 1

  REPO_PATH=$1

  docker container exec $(docker ps --filter name=deployer_agent -q) /run deploy $REPO_PATH
}

"$@"
